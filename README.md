# progetto-so

$ ./run.sh per eseguire il programma.

(OPZIONALE) -c per eseguire il makeFile prima dell'esecuzione del programma
(OPZIONALE) -e -m e specificare numero mappa e etcs mode
(OPZIONALE) -d debug mode